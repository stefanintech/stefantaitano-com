---
layout: base
title: Articles
description: 'Articles about ServiceNow, Ruby, work, and life outside the browser.'
pagination:
  data: collections.allPosts
  size: 8
  generatePageOnEmptyData: true
permalink: 'articles/{% if pagination.pageNumber >=1 %}page-{{ pagination.pageNumber + 1 }}/{% endif %}index.html'
---

<div class="wrapper">
  {% set kicker = "Writing" %}
  {% set intro = "ServiceNow, Ruby, work, and life outside the browser." %}
  {% include "partials/page-intro.njk" %}

  {% if pagination.items.length %}
    <custom-masonry layout="50-50">
      {% asyncEach item in pagination.items %}
        {% include "partials/card-blog.njk" %}
      {% endeach %}
    </custom-masonry>
  {% else %}
    <div class="prose flow region" style="--region-space-top: var(--space-l)">
      <p>No articles published yet. When something is ready, it will show up here.</p>
    </div>
  {% endif %}

  {% set collectionToPaginate = collections.allPosts %}
  {% set metaKey = "blog" %}
  {% if collectionToPaginate.length > pagination.size %}
    {% include 'partials/pagination.njk' %}
  {% endif %}
</div>
